export function webpack(config, { isServer }) {
  //   if (!isServer) {
  //     config.module.rules.push({
  //       test: /\.worker\.ts$/,
  //       loader: 'worker-loader',
  //       options: {
  //         esModule: true,
  //       },
  //     })
  //   }

  return config
}
